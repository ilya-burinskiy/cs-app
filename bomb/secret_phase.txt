/*
 * TODO:
 *   разобраться с кастом указателей, чтобы исследовать двоичное дерево;
 *   решить без брутфорса.
 */
int func7(int *p, int x) {
  if (p == NULL) { return -1; }
  if (*p > x) { return 2 * func7(*(p + 2), x); }
  if (*p == x) { return 0; }
  return 1 + 2 * func7(*(p + 4), x);
}
